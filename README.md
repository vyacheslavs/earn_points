![build](https://github.com/vyacheslavs/earn_points/actions/workflows/node.js.yml/badge.svg)

# Earn points application

Docker build example:

docker build . -t ep:latest

Docker run example:

docker run --name ep --rm -d ep:latest
